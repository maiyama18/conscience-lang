fn add = (a, b) => {
    let c = a + b;
    c;
}

/* this is comment */

let x = 1;
let y = 2;
let z = 3;

if (add(x, y) == z) {
    print "ok!!!";
}

print "count" + "down" + "...";
while (z >= 0) {
    print z;
    z = z - 1;
}

let s = "global";
print "in global scope: " + s;
if (true) {
    let s = "block";
    print "in block scope: " + s;
}
print "in global scope again: " + s;
